# Copiez vers .env a la racine du projet (dossier admin) puis renseignez la cle.
# Format: CLE=valeur   (sans "export", sans guillemets sauf si necessaire)
#
# Supabase > Project Settings > API > service_role (secret)
SUPABASE_SERVICE_ROLE_KEY=
